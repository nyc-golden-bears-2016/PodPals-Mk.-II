<% if @comment.user == current_user %>
  <%= form_for @discussion do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.text_area :content, size: "60x12" %>
    <%= f.submit "Update" %>
  <% end %>
<% end %>